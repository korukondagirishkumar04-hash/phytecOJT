// 3 Write a program to accept two numbers as input and calculate the sum of the two numbers and display the result ? 

#include<stdio.h>
int main()
{
    int a,b,sum;
    printf("enter two numbers:");
    scanf("%d %d",&a,&b);
    sum=a+b;
    printf("sum:%d\n",sum);
}