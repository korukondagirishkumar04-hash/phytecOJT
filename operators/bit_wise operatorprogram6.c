#include <stdio.h>
struct normal{
	char a;
	int b;
	short c;
	char d;
};
int main()
{
	struct normal nor;
	printf("sixze of char : %zu\n",sizeof(char));
	printf("size of int : %zu\n",sizeof(int));
	printf("size of short : %zu\n",sizeof(short));
	print("\nsize of normal struct : %zu bytes\n",sizeof(struct normal)0;
			printf("off set :%zu\n bytes
